{
  "name": "prime-number-check",
  "version": "1.0.0",
  "description": "Check if an integer is a prime number or not",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KaonWry",
  "license": "ISC",
  "dependencies": {
    "prompt-sync": "^4.2.0"
  }
}
